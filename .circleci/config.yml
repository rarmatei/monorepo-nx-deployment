version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-test-and-deploy:
    executor:
      name: node/default
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: echo "current2 << pipeline.git.revision >> base << pipeline.git.base_revision >>"
            - run: npm install
            - run: npm test
            - run: npm affected:deploy
workflows:
  build-test-and-deploy:
    jobs:
      - build-test-and-deploy
